import java.io.*;public class readdata{	// The following function is a placeholder for control initialization.	// You should call this function from a constructor or initialization function.	public void vcInit() {		//{{INIT_CONTROLS		//}}	}	    public static int[] paras;    public static int start,stop;    public static int portno=1500;    public static String hostname="192.168.101.71";    public static plotconn HC;    public static String fileout;        public static void main(String[] args){      if(args.length>=4){       fileout=args[0];       start=plotapp.secs(args[1]);       stop=plotapp.secs(args[2]);       int n=plotapp.countseps(args[3],",");       paras=plotapp.stringsepi(args[3],",",n);       if(args.length>=5)hostname=args[4];       if(args.length>=6)portno=Integer.parseInt(args[5]);       if(paras!=null){        try{          HC=new plotconn(hostname,portno);          HC.getStatus();          if(start>0)start=HC.derindex-(((int)HC.status[0]-start)/3);          if(stop>0)stop=HC.derindex-(((int)HC.status[0]-stop)/3);          if(start==-2)start=1;          float[][] a=HC.getData(paras,start,stop,true);          HC.close();          DataOutputStream fwrite=null;          if(fileout.equals("system")){            fwrite=new DataOutputStream(System.out);          }else{	        File outfile=new File(fileout);	        fwrite=new DataOutputStream(new FileOutputStream(outfile));	      }	      fwrite.writeInt((int)a[0].length);    	  for(int i=0;i<a[0].length;i++){    	    for(int i2=0;i2<paras.length;i2++){    	        fwrite.writeFloat(a[i2][i]);    	    }    	  }    	  fwrite.close();        }catch(IOException ioe){          System.out.println(ioe);        }      }       }//     System.out.println("Goodbye");    }	//{{DECLARE_CONTROLS	//}}}