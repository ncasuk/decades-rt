/** * A tick mark defines what writing goes with a tick at a  * particular value. *  * @author Dave Tiddeman * @version 1 */public class tickmark{    /**     * Value of tick mark.     */    public float value;    /**     * Name of tick mark.     */    public String name;    /**     * Sets up a tick mark with any string as text.     *      * @param val Value where the tick mark goes.     * @param nam Text to go with tick mark.     */        tickmark(float val,String nam){        value=val;        name=nam;    }        tickmark(){        value=0;        name="";    }    /**     * Sets up a tick mark for a time.     *      * @param val Value where the tick mark goes.     * @param time The time in seconds past midnight.     */    tickmark(float val,boolean time){        if(time){           name=gmt(val);        }else{           name=new StringBuffer("").append(val).toString();        }        value=val;    }  /**   * Adds leading zeroes to a number.   *    * @param a Number   * @param n Number of digits in result.   * @return String with leading zeroes added   */      private String lead0(int a,int n){    StringBuffer ans=new StringBuffer();    ans.append(a);    while(ans.length()<n)ans.insert(0,"0");    return ans.toString();  }    public String gmt(float val){    String ans;    int h=(int)(Math.floor(val/3600));    float l=(val-(float)h*3600);    int m=(int)Math.floor(l/60);    l=(l-(float)m*60);    int s=(int)Math.floor(l);    ans=lead0(h,2)+":"+lead0(m,2)+":"+lead0(s,2);    return ans;  }  /**   * Tick mark as a string.   *    * @return Name of tick mark.   */  public String toString(){    return name;  }}